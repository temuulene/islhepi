% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcdata.R
\name{islh_bc_geography}
\alias{islh_bc_geography}
\title{Download BC health-geography boundaries}
\usage{
islh_bc_geography(
  geography = c("lha", "hsda", "chsa", "ha"),
  health_authority = "Vancouver Island",
  crs = 3005,
  simplify_tolerance = NULL
)
}
\arguments{
\item{geography}{One of \code{"ha"}, \code{"hsda"}, \code{"lha"} or \code{"chsa"}.}

\item{health_authority}{Health authority name. \code{"Island Health"} is accepted
as an alias for the catalogue value \code{"Vancouver Island"}. Use \code{NULL} to
return all health authorities.}

\item{crs}{EPSG code for the returned coordinate reference system. BC Albers
(\code{3005}) is the default.}

\item{simplify_tolerance}{Optional non-negative simplification tolerance in
the units of \code{crs}. \code{NULL} keeps the source geometry unchanged.}
}
\value{
An \code{sf} object with \code{geography}, \code{geography_code},
\code{geography_name}, \code{health_authority_name} and \code{geometry}.
}
\description{
Downloads a pinned BC Data Catalogue Web Feature Service through \code{bcdata}
and returns standardized \code{sf} columns. By default, only Island Health
boundaries are returned. Use \code{health_authority = NULL} for all of BC.
}
\examples{
\dontrun{
island_lha <- islh_bc_geography("lha")
bc_chsa <- islh_bc_geography("chsa", health_authority = NULL)
}
}
