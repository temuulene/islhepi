% Generated by roxygen2: do not edit by hand
\name{islh_surveillance_snapshot}
\alias{islh_surveillance_snapshot}
\title{Build a current surveillance snapshot}
\usage{
islh_surveillance_snapshot(
  data,
  date,
  value,
  by = NULL,
  end = NULL,
  periods = 7L,
  interval = c("day", "week", "isoweek", "epiweek", "month", "quarter", "year"),
  week_start = 1,
  baseline = NULL,
  include_total = FALSE,
  total_label = "All"
)
}
\arguments{
\item{data}{A complete count table, normally returned by \code{islh_count_events()}.}
\item{date}{Period date column.}
\item{value}{Whole-count column.}
\item{by}{Optional tidy-select specification of grouping columns.}
\item{end}{Last period to show. Defaults to the latest date in \code{data}.}
\item{periods}{Number of periods to show.}
\item{interval}{Period spacing used to construct the window.}
\item{week_start}{Start of an ordinary week, from 1 (Monday) to 7 (Sunday).}
\item{baseline}{Optional result from \code{islh_surveillance_baseline()}. Its
limits must describe the same duration as the displayed \code{total}. For
example, a seven-day snapshot should use a baseline of historical seven-day or
weekly totals, not historical daily counts.}
\item{include_total}{Add an \code{All} row. Available with one grouping column
and appropriate only for mutually exclusive groups.}
\item{total_label}{Label used for the total group.}
}
\value{
A wide data frame containing groups, \code{total}, optional baseline fields,
\code{exceeds_reference}, and one column per displayed period.
}
\description{
Turns complete period counts into an operational table with one row per group,
recent periods in columns, a current total and optional historical statistics.
}
\examples{
daily <- data.frame(
  site = rep(c("A", "B"), each = 7),
  date = rep(seq(as.Date("2026-08-01"), by = "day", length.out = 7), 2),
  count = c(0, 1, 2, 0, 1, 0, 2, 1, 2, 1, 3, 1, 0, 1)
)

islh_surveillance_snapshot(
  daily,
  date = date,
  value = count,
  by = site,
  periods = 7,
  include_total = TRUE
)
}
