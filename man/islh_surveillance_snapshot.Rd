% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surveillance.R
\name{islh_surveillance_snapshot}
\alias{islh_surveillance_snapshot}
\title{Build a current surveillance snapshot}
\usage{
islh_surveillance_snapshot(
  data,
  date,
  value,
  by = NULL,
  end = NULL,
  periods = 7L,
  interval = c("day", "week", "isoweek", "epiweek", "month", "quarter", "year"),
  week_start = 1,
  baseline = NULL,
  include_total = FALSE,
  total_label = "All",
  baseline_interval = NULL
)
}
\arguments{
\item{data}{A complete count table, normally returned by
\code{\link[=islh_count_events]{islh_count_events()}}.}

\item{date}{Period date column.}

\item{value}{Whole-count column.}

\item{by}{Optional tidy-select specification of grouping columns.}

\item{end}{Last period to show. Defaults to the latest date in \code{data}.}

\item{periods}{Number of periods to show.}

\item{interval}{Period spacing used to construct the window.}

\item{week_start}{Start of an ordinary week, from 1 (Monday) to 7 (Sunday).}

\item{baseline}{Optional result from \code{\link[=islh_surveillance_baseline]{islh_surveillance_baseline()}}. Its
limits must describe the same duration as the displayed \code{total}. For
example, a seven-day snapshot should use a baseline of historical
seven-day or weekly totals, not historical daily counts. Mismatched
durations are refused rather than reported.}

\item{include_total}{Add an \code{All} row. This is only available with one
grouping column and should only be used for mutually exclusive groups.}

\item{total_label}{Label used for the total group.}

\item{baseline_interval}{Duration one \code{baseline} limit describes, named
explicitly. Use it for limits that did not come from
\code{\link[=islh_surveillance_baseline]{islh_surveillance_baseline()}}, such as last season's totals read from a
spreadsheet. \code{NULL} uses the duration the baseline records.}
}
\value{
A wide data frame containing groups, \code{total}, optional baseline
fields, \code{exceeds_reference}, and one column per displayed period.
}
\description{
\code{islh_surveillance_snapshot()} turns complete period counts into the familiar
operational table: one row per group, recent periods in columns, a current
total and optional historical reference statistics.
}
\section{Alert boundary}{


\code{exceeds_reference} is \code{TRUE} when \code{total} is \strong{at or above} \code{upper_limit},
not strictly above it. A total exactly equal to the limit is flagged.

The boundary matters in practice. With \code{method = "range"} the upper limit is
the historical maximum, and with \code{method = "quantile"} it is an observed
value, so both are whole counts that a current total lands on regularly.
Equality is treated as a signal because this column screens for review
rather than deciding anything: a total that has reached the highest value
ever recorded is worth a look, and a missed signal costs more than an extra
one. \code{upper_limit} is \code{NA} when a group has no baseline, and
\code{exceeds_reference} is \code{NA} with it.

A flag is a statistical exceedance and nothing more. It does not say an
outbreak is under way or that action is required.
}

\section{Reporting-period metadata}{


A \code{total} covering seven days cannot be compared with a baseline built from
single daily counts, so the function checks both sides before it reports
anything:
\itemize{
\item \code{data} must hold periods that divide evenly into \code{interval}. Daily counts
can be summed into a weekly snapshot; weekly counts cannot be split into
a daily one.
\item \code{baseline} must describe the same duration as \code{periods} of \code{interval}.
Seven daily columns total one week, so a weekly baseline fits and a daily
baseline does not.
}

Both checks read metadata that \code{\link[=islh_count_events]{islh_count_events()}} and
\code{\link[=islh_surveillance_baseline]{islh_surveillance_baseline()}} leave on their results. When a baseline
carries none, the comparison cannot be verified and is refused; pass
\code{interval} to \code{\link[=islh_surveillance_baseline]{islh_surveillance_baseline()}} to record it, or name the
duration here with \code{baseline_interval}.
}

\examples{
daily <- data.frame(
  site = rep(c("A", "B"), each = 7),
  date = rep(seq(as.Date("2026-08-01"), by = "day", length.out = 7), 2),
  count = c(0, 1, 2, 0, 1, 0, 2, 1, 2, 1, 3, 1, 0, 1)
)

islh_surveillance_snapshot(
  daily,
  date = date,
  value = count,
  by = site,
  periods = 7,
  include_total = TRUE
)

}
