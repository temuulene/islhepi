% Generated by roxygen2: do not edit by hand
\name{islh_check_events}
\alias{islh_check_events}
\title{Find record-level problems in event data}
\usage{
islh_check_events(
  data,
  id,
  date,
  required = NULL,
  one_row_per_id = TRUE,
  min_date = NULL,
  max_date = Sys.Date()
)
}
\arguments{
\item{data}{A data frame containing one row per event or encounter.}
\item{id}{Column containing the event identifier.}
\item{date}{Column containing the event date. Date, POSIXt and ISO
\code{YYYY-MM-DD} character values are supported.}
\item{required}{Optional tidy-select specification of fields that must be
present and non-missing.}
\item{one_row_per_id}{Whether repeated non-missing identifiers are problems.}
\item{min_date, max_date}{Optional inclusive date limits. \code{max_date}
defaults to today; use \code{NULL} when future-dated records are expected.}
}
\value{
A data frame with \code{.row}, \code{.id}, \code{.issue}, \code{.field} and
\code{.value}. A valid input returns a zero-row data frame with the same columns.
}
\description{
Returns one row for every problem it finds. It does not alter the input or hide
the affected records, making the result suitable for review with a data owner.
}
\examples{
events <- data.frame(
  record = c("A1", "A1", "", "A4"),
  onset = c("2026-01-03", "2026-01-04", NA, "not a date"),
  condition = c("Influenza", "Influenza", "COVID-19", "")
)

islh_check_events(
  events,
  id = record,
  date = onset,
  required = condition,
  max_date = NULL
)
}
