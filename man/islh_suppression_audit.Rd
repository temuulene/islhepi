% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase-suppress.R
\name{islh_suppression_audit}
\alias{islh_suppression_audit}
\title{Read the record of what a suppression hid}
\usage{
islh_suppression_audit(data)
}
\arguments{
\item{data}{A data frame returned by \code{\link[=islh_suppress_table]{islh_suppress_table()}}.}
}
\value{
A data frame with columns \code{column}, \code{row}, \code{group} and \code{reason}.
\code{reason} is \code{"small"} for a cell the threshold caught and
\code{"complementary"} for one hidden to protect another. \code{group} is the block
the complementary rule ran within, or \code{NA} when no \code{by} was used. A table
with nothing hidden gives a zero-row data frame.
}
\description{
Returns one row for every cell \code{\link[=islh_suppress_table]{islh_suppress_table()}} hid, saying which
cell it was and why it was hidden. Keep it with your working notes so a
reviewer can confirm the rule behaved as intended.
}
\details{
The record holds no counts. It says a cell was hidden, not what was in it,
so it is safe to keep alongside a released table.
}
\examples{
counts <- data.frame(
  area = c("North", "Central", "South"),
  cases = c(3, 42, 17)
)

hidden <- islh_suppress_table(
  counts, "cases", threshold = 5,
  complementary = TRUE, label = "Suppressed"
)
islh_suppression_audit(hidden)
}
