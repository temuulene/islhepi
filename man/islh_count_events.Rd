% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surveillance.R
\name{islh_count_events}
\alias{islh_count_events}
\title{Count events in complete reporting periods}
\usage{
islh_count_events(
  data,
  date,
  id = NULL,
  count = NULL,
  by = NULL,
  interval = c("day", "week", "isoweek", "epiweek", "month", "quarter", "year"),
  week_start = 1,
  from = NULL,
  to = NULL,
  fill = TRUE,
  include_partial = FALSE,
  groups = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{date}{Date column.}

\item{id}{Optional identifier column. When supplied, distinct non-missing IDs
are counted. When neither \code{id} nor \code{count} is supplied, rows are counted.}

\item{count}{Optional column of pre-aggregated whole counts. \code{id} and \code{count}
cannot both be supplied.}

\item{by}{Optional tidy-select specification of grouping columns.}

\item{interval}{Reporting interval: \code{"day"}, \code{"week"}, \code{"isoweek"},
\code{"epiweek"}, \code{"month"}, \code{"quarter"} or \code{"year"}.}

\item{week_start}{Start of an ordinary week, where 1 is Monday and 7 is
Sunday. ISO and epidemiological weeks always use Monday and Sunday,
respectively.}

\item{from, to}{Optional inclusive date boundaries. Defaults to the observed
date range.}

\item{fill}{Whether to add zero-count periods.}

\item{include_partial}{Whether to retain a final period whose end is after
\code{to}.}

\item{groups}{Optional expected groups. For one \code{by} column this may be a
vector; for multiple columns supply a data frame containing valid group
combinations. This is how groups with no events anywhere in the selected
window can still be shown.}
}
\value{
A data frame containing the grouping columns, \code{period_start},
\code{period_end}, \code{count} and \code{partial_period}. The result carries the
reporting period it was built with; see the reporting-period section.
}
\description{
\code{islh_count_events()} converts event-level or already aggregated data into a
consistent period table. It handles epidemiological and ISO weeks, constructs
missing zero-count periods and marks periods that extend beyond the reporting
cutoff.
}
\section{Reporting-period metadata}{


The result carries \code{interval}, \code{week_start} and the \code{from}-\code{to} window as
attributes. \code{\link[=islh_surveillance_baseline]{islh_surveillance_baseline()}} and
\code{\link[=islh_surveillance_snapshot]{islh_surveillance_snapshot()}} read them to check that a comparison covers
the same duration on both sides, so a seven-day snapshot cannot be measured
against a baseline of single daily counts.

Subsetting a data frame drops these attributes. Pass the result straight to
the next function, or give that function an explicit \code{interval} argument if
you have reshaped the table in between.
}

\examples{
events <- data.frame(
  encounter = 1:6,
  event_date = as.Date("2026-01-01") + c(0, 0, 2, 8, 8, 15),
  region = c("North", "North", "South", "North", "South", "North")
)

islh_count_events(
  events,
  date = event_date,
  id = encounter,
  by = region,
  interval = "week",
  week_start = 7,
  fill = TRUE,
  include_partial = TRUE
)

}
