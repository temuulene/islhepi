% Generated by roxygen2: do not edit by hand
\name{islh_count_events}
\alias{islh_count_events}
\title{Count events in complete reporting periods}
\usage{
islh_count_events(
  data,
  date,
  id = NULL,
  count = NULL,
  by = NULL,
  interval = c("day", "week", "isoweek", "epiweek", "month", "quarter", "year"),
  week_start = 1,
  from = NULL,
  to = NULL,
  fill = TRUE,
  include_partial = FALSE,
  groups = NULL
)
}
\arguments{
\item{data}{A data frame.}
\item{date}{Date column.}
\item{id}{Optional identifier column. When supplied, distinct non-missing IDs
are counted. When neither \code{id} nor \code{count} is supplied, rows are counted.}
\item{count}{Optional column of pre-aggregated whole counts. \code{id} and
\code{count} cannot both be supplied.}
\item{by}{Optional tidy-select specification of grouping columns.}
\item{interval}{Reporting interval.}
\item{week_start}{Start of an ordinary week, where 1 is Monday and 7 is Sunday.}
\item{from, to}{Optional inclusive date boundaries. Defaults to the observed range.}
\item{fill}{Whether to add zero-count periods.}
\item{include_partial}{Whether to retain a final period whose end is after \code{to}.}
\item{groups}{Optional expected groups. For one \code{by} column this may be a
vector; for multiple columns supply a data frame containing valid combinations.}
}
\value{
A data frame containing grouping columns, \code{period_start},
\code{period_end}, \code{count} and \code{partial_period}.
}
\description{
Converts event-level or already aggregated data into a consistent period table.
It handles epidemiological and ISO weeks, constructs missing zero-count periods
and marks periods extending beyond the reporting cutoff.
}
\examples{
events <- data.frame(
  encounter = 1:6,
  event_date = as.Date("2026-01-01") + c(0, 0, 2, 8, 8, 15),
  region = c("North", "North", "South", "North", "South", "North")
)

islh_count_events(
  events,
  date = event_date,
  id = encounter,
  by = region,
  interval = "week",
  week_start = 7,
  fill = TRUE,
  include_partial = TRUE
)
}
